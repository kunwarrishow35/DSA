class Solution:
    def majorityElement(self, arr):
        freq = {}

        for x in arr:
            freq[x] = freq.get(x, 0) + 1

        for x in freq:
            if freq[x] > len(arr) // 2:
                return x

        return -1