class Solution:
    def getMinMax(self, arr):
        # code here
        min = max = arr[0]
        for i in arr:
            if i>max:
                max = i
            if i<min:
                min = i
        return [min, max]