class Solution:
    def checkElements(self, start, end, arr):
        new = []

        for i in range(start, end + 1):
            new.append(i)

        a = set(arr)
        b = set(new)

        return b.issubset(a)
