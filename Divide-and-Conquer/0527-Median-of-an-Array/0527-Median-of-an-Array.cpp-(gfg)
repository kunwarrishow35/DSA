class Solution:
    def findMedian(self, arr):
        #code here.
        n = len(arr)
        arr.sort()
        if n%2==0:
            median = (arr[n//2]+arr[n//2-1])/2
        else:
            median = arr[abs(n//2)]
        return median